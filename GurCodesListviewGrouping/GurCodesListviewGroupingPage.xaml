<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:GurCodesListviewGrouping" x:Class="GurCodesListviewGrouping.GurCodesListviewGroupingPage">
	<ListView ItemsSource="{Binding Items}" IsGroupingEnabled="true" GroupShortNameBinding="{Binding Key}" GroupDisplayBinding="{Binding Key}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Name}" Detail="{Binding AlterEgo}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>